- var theme_customizer      = true;
- var tooltip               = true;
- var jsgrid                = true;
- var sidebar               = true;
- var scrollbar             = true;
doctype html
html(lang='en')
  include ../../components/header-files
  body
    include ../../components/taptop
    include ../../components/loader
    // page-wrapper Start
    #pageWrapper.page-wrapper.compact-wrapper
      include ../../components/header
      // Page Body Start
      .page-body-wrapper
        include ../../components/sidebar
        .page-body
          .container-fluid
            .page-title
              .row
                .col-sm-6
                  h3
                    | JS Grid Tables
                .col-sm-6
                  ol.breadcrumb
                    li.breadcrumb-item
                      a(href='index.html')
                        i(data-feather="home")
                    li.breadcrumb-item Tables
                    li.breadcrumb-item.active  JS Grid Tables
          // Container-fluid starts
          .container-fluid
            .row
              .col-sm-12
                .card
                  .card-header.pb-0
                    h4 Basic Scenario
                    span
                      | Grid with filtering, editing, inserting, deleting, sorting and paging. Data provided by controller.
                  .card-body
                    #basicScenario
              .col-sm-12
                .card
                  .card-header.pb-0
                    h4.mb-3 Sorting Scenario
                    span
                      | Sorting can be done not only with column header interaction, but also with sort method.
                  .card-body
                    .sort-panel.mb-3
                      label
                        | Sorting Field:
                        select#sortingField.btn.btn-info.dropdown-toggle.btn-md.text-start
                          option Id
                          option Product
                          option Order Id
                          option Price
                          option Shipped
                      .d-inline
                        button#sort.btn.btn-md.btn-secondary(type='button') Sort
                    #sorting-table.js-shorting
              .col-sm-12
                .card
                  .card-header.pb-0
                    h4.mb-3 Batch Delete
                    span
                      | Cell content of every column can be customized with itemTemplate, headerTemplate, filterTemplate and insertTemplate functions specified in field config. This example shows how to implement batch deleting with custom field for selecting items.
                  .card-body
                    #batchDelete
          // Container-fluid Ends
        include ../../components/footer
    include ../../components/footer-files
